// 이 파일은 이전에 saveConfig를 가로채서 학년별 시수를 보정하던 패치입니다.
// 자동배정 화면 멈춤 가능성이 있어 비활성화했습니다.
// 학년별 시수 반영은 src/optimizer-ui.js의 applyOptimizerResult 내부에서 직접 처리하는 방식으로 옮깁니다.

window.addEventListener('DOMContentLoaded', () => {
  // no-op
});
